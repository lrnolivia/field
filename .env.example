# ─── field editor - environment ─────────────────────────────────────────────
# Normal `vite` development uses LocalBackend/localStorage. A normal production
# build uses the field-native same-origin Worker/R2 backend. Set this only when
# you intentionally want a locally-served production build to stay local.
# VITE_FIELD_LOCAL_BACKEND=true

# Revyme Cloud mode - set BOTH to use the upstream Revyme cloud backend instead
# of field persistence. Must be exactly the string "true".
# VITE_REVYME_CLOUD=true
# VITE_API_URL=http://localhost:8090

# Cloudflare Worker runtime (NOT Vite browser variables). Configure these on
# the deployed Worker / environment so project API requests can validate the
# Cloudflare Access assertion. The Worker fails closed when either is absent.
# FIELD_ACCESS_TEAM_DOMAIN=https://your-team.cloudflareaccess.com
# FIELD_ACCESS_AUD=your-access-application-audience-tag

# Optional third-party media/search keys. Features degrade gracefully when
# unset: the font picker falls back to a built-in list (font LOADING never
# needs a key), the Unsplash/Pixabay search tabs hide (upload + URL paste
# always work).
# VITE_GOOGLE_FONTS_KEY=
# VITE_UNSPLASH_ACCESS_KEY=
# VITE_PIXABAY_KEY=

# AI features (chat panels, page agent, MCP bridge) expect a compatible
# service at this URL (not part of this repo). Leave unset to keep the UI
# inert - it degrades with error toasts, nothing else breaks.
# VITE_AI_SERVICE_URL=http://localhost:8082

# Self-hosting overrides.
# VITE_CDN_HOST=https://assets.revyme.app
# VITE_PLATFORM_HOST=https://platform.revyme.app
# Comma-separated Host allowlist for `vite preview` behind a proxy.
# VITE_ALLOWED_HOSTS=example.com,.example.com

# Preview server (`npm run dev` boots it on 5175; Next-based page preview
# uses 5173 when running the separate preview project).
# VITE_PREVIEW_HOST=http://localhost:5173

# Site import service — the endpoint DebugToolbar’s "Import" posts to
VITE_IMPORT_SERVICE_URL=http://localhost:5311
