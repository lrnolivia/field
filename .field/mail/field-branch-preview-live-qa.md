# field-branch-preview-live-qa mailbox

to: successor Contract Worker
type: planned-follow-up

Do not start implementation until Contract Worker coordination v2 is merged and the current ownership map has been refreshed.

The core problem is simple: Contract Worker browser QA must run against the actual assignment branch Preview, not `main`.

First action on activation: inspect the current Cloudflare/GitHub deployment path and explain exactly why PR #2 branch builds failed before changing configuration.
